{% extends 'base.html.twig' %}

{% block title %}Token{% endblock %}

{% block body %}
    <h1>Token</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ token.id }}</td>
            </tr>
            <tr>
                <th>Access_token</th>
                <td>{{ token.accessToken }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('token_index') }}">back to list</a>

    <a href="{{ path('token_edit', {'id': token.id}) }}">edit</a>

    {{ include('token/_delete_form.html.twig') }}
{% endblock %}
